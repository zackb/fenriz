# Config for the `kde-blur` scenario. Blur is off in the compiled-in defaults, and a
# compositor with it off advertises no blur capability — so without this file the scenario
# would be asserting against the wrong half of the feature.
blur = true
blur_passes = 2
blur_radius = 5

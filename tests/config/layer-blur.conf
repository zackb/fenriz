# Config for the `layer-blur` scenario. A bar asking for blur is the feature's whole point, and
# a layer surface is the awkward case: its blur nodes are siblings of its subtree rather than
# children, so they have to be moved, hidden and destroyed by hand.
blur         = true
animation_ms = 0

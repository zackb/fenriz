<?xml version="1.0" encoding="UTF-8"?>
<protocol name="blur">
  <copyright>
    SPDX-FileCopyrightText: 2015 Martin Gräßlin
    SPDX-License-Identifier: LGPL-2.1-or-later
  </copyright>

  <!--
    org_kde_kwin_blur, from plasma-wayland-protocols. Vendored because that package is not a
    fenriz dependency and the protocol is not part of wayland-protocols.

    This is the predecessor of ext-background-effect-v1 and does the same job with no
    capabilities event. It is here because it is what clients on the ground actually speak:
    Ghostty asks for blur through this and nothing else, as do KDE apps that have not moved to
    the standard protocol. Both feed the same blur machinery in src/background_blur.cpp; prefer
    ext-background-effect-v1 in anything new.
  -->

  <interface name="org_kde_kwin_blur_manager" version="1">
    <request name="create">
      <arg name="id" type="new_id" interface="org_kde_kwin_blur"/>
      <arg name="surface" type="object" interface="wl_surface"/>
    </request>
    <request name="unset">
      <arg name="surface" type="object" interface="wl_surface"/>
    </request>
  </interface>

  <interface name="org_kde_kwin_blur" version="1">
    <request name="commit"/>
    <request name="set_region">
      <arg name="region" type="object" interface="wl_region" allow-null="true"/>
    </request>
    <request name="release" type="destructor">
      <description summary="release the blur object"/>
    </request>
  </interface>
</protocol>
